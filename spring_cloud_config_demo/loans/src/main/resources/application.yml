spring:
  application:
    name: "loans"
  profiles:
    active: "qa"
  config:
    import: "configserver:http://localhost:8071/"
  rabbitmq:
    host: "127.0.0.1"
    port: "5672"
    username: "guest"
    password: "guest"

management:
  endpoints:
    web:
      exposure:
        include: "*"



