spring:
  application:
    name: "loans"
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:default}
  config:
    import: ${SPRING_CONFIG_IMPORT:configserver:http://localhost:8071/}

management:
  endpoints:
    web:
      exposure:
        include: "*"



